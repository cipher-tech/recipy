import React from 'react';
import { SearchForm } from '../../components/form/searchForm/SearchForm';
import './home.scss';

export const Home = () => {
      return (
            <section>
                  <SearchForm />
            </section>
      );
};
