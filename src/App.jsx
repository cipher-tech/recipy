import { Fragment } from 'react';
// import { Pages } from './Pages';
import { Pages } from '../src/Pages';

export const App = () => {
      return (
            <Fragment>
                  <Pages />
            </Fragment>
      );
};
